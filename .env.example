# This repo has two apps: frontend/ (Next.js) and backend/ (Express).
#
# Frontend: copy frontend/.env.example to frontend/.env.local and set NEXT_PUBLIC_API_URL.
# Backend:  copy backend/.env.example to backend/.env and set DATABASE_URL, etc.
#
# See frontend/.env.example and backend/.env.example for details.
