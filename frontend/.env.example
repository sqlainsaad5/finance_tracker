# Frontend (Next.js) – copy to .env.local
# For Vercel production, set NEXT_PUBLIC_API_URL in Project Settings → Environment Variables
# to your deployed API URL (e.g. https://your-api.railway.app) with no trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:4000
